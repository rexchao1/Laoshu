import SwiftUI

/// The look decided in D28/D29: a cool near-white ground, a green accent for
/// progress and the affirmative side of a swipe. Nothing else in the app
/// picks its own colors.
enum LaoshuTheme {
    static let background = Color(red: 0.95, green: 0.96, blue: 0.98)
    static let accent = Color(red: 0.20, green: 0.68, blue: 0.42)
}
